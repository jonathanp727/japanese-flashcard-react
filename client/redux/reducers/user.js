import {
} from '../actions';

const initialState = {
  name: null
};

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userReducer;
